# Cottonquant V5.1 任务书

本文件是 `CottonquantV5任务书.md` 的修正版和唯一执行依据。原 V5 文件作为设计草案保留，
不再作为工程验收标准。

## 一、项目定位

Cottonquant V5.1 是 **CF 策略可问责研究工作台**，不是实盘交易平台。系统必须把研究信号
转换为可复算的历史策略和不可静默回改的前向影子记录，以统一标准回答：研究模块是否真正
改善成本后收益、风险和稳定性。

第一阶段严格限制为 CF。禁止自动下单、OMS、分钟级执行、实时推送和多品种接入。所有输出
均为研究仿真，不构成交易指令。

## 二、不可违反的时序

1. T 日结算后只生成目标仓位。
2. 目标最早在下一官方交易日 T+1 的结算价成交。
3. 新仓位不获得 T 到 T+1 的价格变化，只从 T+1 到 T+2 开始产生损益。
4. 连续价格只用于信号，成交、持仓、成本和损益必须使用 trade mapping 指向的真实合约。
5. 最新日尚无下一官方交易日时，目标状态为 `PENDING_NEXT_OFFICIAL_SESSION`。

## 三、固定研究参数

以下参数已经于 2026-07-28 确认为研究专用参数：

| 参数 | 数值 |
| --- | --- |
| 记账资本 | 1,000,000 CNY |
| 目标年化波动 | 12% |
| 年化波动下限 | 5% |
| 最大仓位 | 20 手 |
| no cost | 单边 0 bps |
| normal cost | 单边 5 bps |
| conservative cost | 单边 10 bps |

这些值不代表真实账户配置。参数变更必须升级 spec version 并写入 changelog。

## 四、R86-R93 实施主线

### R86 规格和输入基座

- 建立 Pydantic 策略规格和 CF-only 注册表。
- 递归拒绝 `forward_return`、`fwd_ret`、`future_*` 输入。
- 补齐 2021-2023 郑商所官方 CF 历史行情衍生交易日历。
- 复用现有 chain map、trade mapping 和 continuous price 算法，生成统一跨年度输入；CF策略映射显式限制为01/05/09主力周期，03/07/11不得因交割或套保持仓短暂放大而进入方向信号连续价。
- CLI：`strategy validate-specs`、`strategy prepare-inputs`。

### R87 基准策略

- `CF_tsmom_v0` 使用 20 日调整后连续结算价动量。
- 波动率使用截至 T 日的最近 20 个完整日收益；不足 21 个价格时空仓。
- 目标仓位使用波动目标模型，转换为 `BacktestTargetLotDailyRow` 后调用现有回测引擎。
- 成本按每笔成交名义金额乘单边 bps 计算，换月自然产生平旧和开新两笔成本。

### R88 策略评估

- 输出完整年度、两年滚动窗口、全历史和 2026 YTD 诊断。
- 指标包括收益、波动、Sharpe、最大回撤、Calmar、换手、在场时间、持仓周期和阶段归因。
- 两年滚动窗口只用于诊断，不作为独立晋级样本。

### R89 候选策略

- 固定使用 `signal_matrix_daily` 的 20D 行和 `trend_phase_v2_daily`。
- 期权背离必须同时满足背离标签和显式 `option_signal_direction` 与期货方向相反。
- 字段语义冲突时不施加期权门控，并输出 warning。
- 晋级使用 2021-2025 五个互不重叠完整年度；不得赛后改参重跑。

晋级条件：至少四个合格年度 Sharpe 不低于基准；每个合格年度至少 60 个在场日和 4 个
完成持仓事件；全历史 conservative cost `Delta Sharpe >= 0.10`、累计收益为正，最大回撤
相对基准恶化不超过 5 个百分点。

### R90 前向影子台账

- 2026-08-06主线决策：R90从日更、周更和后续晋级门槛中移除。现有事件和账本只保留为历史审计证据，不再续写，不删除，也不用于判断研究系统是否有效。
- 从启用日零仓位开始，不使用历史回测仓位初始化。
- 不可变逐日事件为事实源，Parquet 是原子重建的物化视图。
- 相同重跑为 no-op；修复只能追加 correction event，必须引用前序 checksum 和原因。
- 历史回放必须标记 `HISTORICAL_REPLAY`，不得计入 `FORWARD_CAPTURE` 表现。

### R91-R93 治理和展示

- R91 每周汇总影子表现和账务异常，日更不运行完整审计。
- R91 已实现：最近五行表现与累计前向证据分离，并核对 NAV 连续性、修订事件和相对基准差异。
- R92 用相同账本和晋级规则检验期权、会员持仓和行权价 OI 墙 overlay。
- R92 已实现并固定结论：期权否决 `WATCH`、会员 Top20 `REJECT`、同侧 OI 墙 `WATCH`；不解锁调参，不修改 composite_score。
- R93 在现有单文件仪表盘中增加策略影子卡，不读取 forward return。
- R93 已实现：有可核对台账时展示目标、方向、NAV、回撤和近 60 日曲线；无台账或发现未来标签时整段隐藏。

### R93A CF 多空对称趋势结构研究扩展

- `CF_phase_gated_v0` 继续保持 `FROZEN`，不回改参数，不接入现有影子台账。
- 将趋势方向、生命周期阶段和趋势力度拆分，避免旧口径把强空向确认同时编码为 `S4` 终点。
- 使用 20/40 日价格结构、均线和双价格状态对称识别 long/short；期权与全链持仓只标记确认或背离。
- 独立输出突破事件的 1/3/5/10/20 日历史后验表现，以及完整趋势 episode 的持续期、MFE 和 MAE。
- R93A 不定义目标手数；只有完成消融、滚动样本外验证并重新预登记的新候选，才允许进入独立影子观察。
- R93A 不单独解除 R94-R99 门槛；扩展仍需等待CF历史证据、事件生命周期、期货—期权联动和样本外候选验证通过并单独审批。

### R93B 趋势环境与期权结构节奏增量研究

- 以 R93A 独立趋势 episode 的首次突破为研究单位，不把同一趋势内的重复突破当成独立样本。
- 只使用突破 T 日及以前可观察的波动环境、趋势力度、IV、skew、PCR OI、期权方向、持仓参与和行权价 OI 墙特征。
- skew 与 PCR OI 必须先转换为相对 long/short 趋势方向的支持、背离或中性状态，不混合解释原始符号。
- 每个结构分组与同周期其余独立 episode 做 Fisher 精确检验；绝对二项检验不再冒充增量证据，全部结构检验统一做 Benjamini-Hochberg FDR 校正。
- 事件后的 1/3/5/10/20 日方向收益只作为历史后验标签；日度当前结构不读取这些收益。
- 输出独立事件特征、增量汇总、候选排名、warning、manifest 和中文报告；期权或 OI 墙覆盖落后必须显式报警。
- R93B 只发现研究候选，不定义仓位，不修改 `CF_tsmom_v0`，不解冻 `CF_phase_gated_v0`，不解除 R94-R99 门槛。

### R93C 候选消融、稳定性与前向预登记

- 固定三项候选：持仓参与确认 20D、弱期权环境 5D 失败观察、方向侧 OI 墙增仓 3D；禁止继续从同一历史样本追加规则。
- 规格文件固定候选字段、方向、主周期、实际效应门槛、bootstrap 参数和 `2026-07-28` 前向生效边界。
- 历史检验必须拆分全样本、多空方向、早晚样本、年度和留一年结果，并报告方向×时期标准化效应。
- bootstrap 与留一年结果仍属于候选发现后的回顾性诊断，不能消除数据选择偏误，不得写成样本外证明。
- 生效边界前后的事件必须物理分表；历史结果不得因后续前向标签变化而重算或改判。
- 只有持仓参与确认可进入冻结后的前向登记；弱期权候选只作小样本前向观察，OI 墙候选保留历史观察。
- R93C 不新建策略、不调整影子手数、不修改 `composite_score`，也不解除 R94-R99 门槛。

### R93D 不可回写的候选前向事件账本

- 只跟踪 R93C 的持仓参与确认和弱期权观察，OI 墙历史观察项不进入前向账本。
- 新突破发生当日先追加 `CAPTURE` 事件；CAPTURE 只能包含 T 日事实，禁止包含 exit date、outcome 或收益。
- 5D/20D 标签形成后追加引用 CAPTURE checksum 的 `OUTCOME`；相同重跑必须 no-op。
- 事件日后才首次看到的记录标为 `LATE_BACKFILL_CAPTURE`，保留审计但不计入严格前向证据。
- 已捕获的 T 日特征不得回写；结果变化必须提供 correction reason，并追加引用前序 checksum 的修订事件。
- Parquet 只作为不可变 JSON 事件的原子物化视图；历史 CAPTURE 行在结果追加后仍必须保持不变。
- R93D 只积累证据，不定义前向晋级门槛，不修改策略、影子手数、NAV 或 `composite_score`。

### R93E 周度候选证据汇总

- R90影子通道已退出主线；R93E只汇总R93D预登记趋势候选事件和可追溯结果，不再报告影子交易日门槛。
- 候选通道按预登记假设汇总严格 CAPTURE、迟到补录、5D/20D 待结算与已结算 OUTCOME、修订链。
- 无新突破时只报告采集健康度；未到期事件不得提前判断成功或失败，达到 40 日也不自动证明策略有效。
- R93E 不设置自动晋级、不改变方向或仓位；仅在周更或显式运行时生成中文报告、JSON、Parquet 和 manifest。

### R93F 无换月跳空收益指数与价格测量对照

- 新建独立研究侧车，按真实映射合约的相邻结算价构造收益并复利为基准 100 指数；不覆盖加法连续价。
- 非换月日使用同一真实合约收益；换月日使用旧合约从上一交易日到当日的收益，剔除新旧合约同日价差；下一交易日起改用新合约收益。
- 使用与 `CF_tsmom_v0` 相同的 20 日动量、20 日波动率、目标波动和手数上限，对照方向、波动率及研究测量手数。
- 2021-01-04 至 2026-07-30 的首轮结果：1,330 个完整窗口内方向分歧为 0，目标手数分歧 860 日（64.66%）；无跳空口径相对加法口径的波动率中位比为 1.1556，最大手数绝对差为 3。
- 最新 2026-07-30 两种口径方向均为 `short`，加法口径为 -17 手，无跳空口径为 -15 手。该结果说明主要偏差在风险尺度和仓位，而不是本样本内的方向符号。
- R93F 只做测量诊断，不修改 `CF_tsmom_v0`、R90 影子台账或既有回测。新口径若进入策略，必须建立新 spec 并完成独立 T+1 真实合约回测和人工审批。
- CLI：`strategy build-roll-neutral-return-index`。

### R93G 棉花年度与政策参考线研究

- 棉花年度按每年9月1日至次年8月31日划分，分别汇总真实主力结算价、CCIndex 3128B现货、基差代理及基本面观察。
- `18,600元/吨`只作为用户确认的政策研究参考线；官方有效期、标准级和补贴结算口径仍为 `HUMAN_REVIEW_REQUIRED`，不得解释为期货支撑位、现货保底价或公允价值。
- 价格水平比较只使用CCIndex现货和R86映射真实合约结算价；加法调整连续价与无换月跳空收益指数被配置显式排除。
- 当前可观察偏离表不含未来标签；5/20/60日收敛路径只写入独立历史后验表，同合约期货与现货分别验证。
- 2021-01-04至2026-07-30更新结果：真实映射期货87.11%的交易日低于参考线，接续后的可用3128B现货86.81%的交易日低于参考线，证明参考线不具备市场硬底含义。
- 最新CF609结算价15,725元/吨，较参考线低2,875元/吨（-15.46%）；iFinD 3128B现货为2026-07-30的17,442元/吨，较参考线低1,158元/吨，现货与期货基差代理为1,717元/吨。
- 原CCIndex与iFinD在2025-07-01至2026-07-03有251个重叠观察，相关系数0.9974，iFinD减原序列的中位价差为-44元/吨；该结果只支持向后接续，不证明两个供应商口径完全一致。
- 历史收敛表使用重叠日度观察，样本数不是独立事件数；比例区间只作描述，不作为政策因果或策略晋级证据。
- 轮入轮出、进口配额、补贴结算、新棉产量等政策供给事件尚未形成统一时点表，本版不构造政策供给评分，也不读取独立的 `reserve_cotton2026/` 项目。
- CLI：`research build-cf-cotton-year-policy-research`。

### R93H iFinD EDB 研究数据基座

- 使用用户确认并经真实调用验证的 `THS_EDB` 指标，规范化3128B现货、储备棉抛储、纱线价格和USD/CNY掉期曲线，保留指标ID、发布日期、抓取时间、来源文件和SHA256。
- iFinD EDB只进入研究观察层，不进入 `composite_score`、策略方向或R90影子账本；USD/CNY掉期曲线不得替代即期汇率。
- EDB接口未返回单位元数据，现货、投放量、成交比例和掉期点单位继续标记 `HUMAN_REVIEW_REQUIRED`；储备棉未公布期间不补零。
- R93G允许通过 `--spot-extension-path` 使用R93H现货表，但只接续原CCIndex主序列最后日期之后的数据，重叠历史不覆盖，并在日表中保留来源切换和原始指标ID。
- 2026-08-01首轮真实接入：棉花/政策/纱线1,386行、9个指标，USD/CNY掉期曲线5,035行、19个期限，均无空值、重复和未来日期；THS_BD合约快照因关键字段大量为空被拒绝接入。
- CLI：`research connect-cf-ifind-edb-context`。

### R93I 主力周期期权接力与反弹生命周期

- CF方向研究的主力合约只在01/05/09周期内按持仓量、成交量识别；03/07/11仍进入全链、曲线、交割和套保结构观察，但不得替代研究主力。
- 当期货主力对应期权因临近到期或质量原因不为 `READY` 时，只允许接到下一个01/05/09主力周期。候选必须同时满足 `READY`、有效期权不少于4个、期权流动性分数不低于20、对应期货持仓份额和成交份额均不低于10%；不满足时保持观察缺口，不得回退到11月等中间合约。
- 期货研究主力、期权采用标的、接力原因、期限差以及期权标的对应期货持仓/成交和份额必须分别留存。CF609失效后的优先接力对象是CF701，不是CF611。
- 反弹状态固定为：中长期至少两项偏多且1D/3D/5D至少两项偏空进入 `PREPARE`；1D与3D翻多并收复5日均线进入 `TRIGGER`；1D/3D/5D全部翻多且期权或全链持仓至少一项确认进入 `CONFIRM`；跌破准备低点或短周期与风险证据重新共振转空进入 `FAIL`。
- 日度状态只使用T日及以前数据。T+1收益、MFE、MAE和波动率障碍只写入独立历史后验表，不进入最新信号和 `composite_score`。
- 2021-01-04至2026-08-06首轮识别63个独立准备episode、54个触发和28个确认；当前episode为7月29日准备、8月4日触发、8月5日确认。
- 总体结果不得覆盖年度失效：2024年20D命中率约23.5%、平均收益约-1.75%，因此首版稳定性为 `WEAK_OR_UNSTABLE`，不得晋级为策略规则。
- CLI：`research build-cf-rebound-lifecycle`。

### R93J 基本面数据可用性与新鲜度状态

- 汇总R53长期基本面表与R93H iFinD EDB侧车，形成指标级覆盖、新鲜度、来源、单位、缺口和下一步动作表。
- 日频、周频、月频分别使用3/7日、10/21日、45/75日历日阈值标记 `CURRENT/LAGGING/STALE`；事件型数据标记 `EVENT_DRIVEN`，未公布期间不补零。
- 月频进口和库存、周频纺织链保留原观察日期，不向前填充为伪日频；缺失的纺织订单、棉纱利润、USD/CNY即期、ICE棉花和仓单质量明细显式列为 `MISSING`。
- 日更只读取本地规范化产物重算状态，不调用iFinD；R93J不生成基本面方向，不进入signal matrix或 `composite_score`。
- CLI：`research build-cf-fundamental-data-status`。

### R93K 基本面发布时间对齐与趋势增量研究

- 以R93A独立趋势episode的首次突破为事件单位，只使用事件日及以前可知的基本面记录；5D、20D趋势收益只作为历史后验标签。
- iFinD来源 `rtime` 记为 `RELEASE_DATE_EXACT`；长期基本面表只有观察日期时记为 `OBSERVATION_DATE_PROXY`，两类证据物理分组，不混算。
- 15:00及以后发布的数据顺延至下一CF官方交易日；未知历史发布日期分别按0、5、10个CF交易日做代理滞后敏感性。
- 同指标同周期的当前分组与其余独立episode使用Fisher精确检验，并按证据质量、代理滞后和周期分别进行Benjamini-Hochberg FDR校正。
- 已确认的规范指标改名按数据类别与规范指标名拼接为一条连续序列，事件行继续保留当期原始指标ID，禁止口径切换前后重复计票。
- 2021-03-19至2026-07-30首轮真实研究覆盖55个独立episode、109个episode-周期行、126条严格事件特征和3,597条代理事件特征；严格证据无正向候选、无负向过滤器，代理关系在FDR和0/5/10日滞后下均不稳健。
- 代理证据无论表面效果如何均不得晋级策略；R93K不生成 `fundamental_signal`，不修改signal matrix、`composite_score`或策略方向。
- 默认进入周更研究包，也可通过 `-RunFundamentalTrendIncremental` 单独运行；日更只保留R93J新鲜度状态，不运行R93K全历史检验。
- CLI：`research build-cf-fundamental-trend-incremental-research`。

### R93L 趋势突破、全链持仓与期权确认时序研究

- 以R93A每个独立趋势episode的首次突破为事件锚点，观察T-10至T+20交易日；轨迹日不冒充独立统计样本。
- 全链持仓确认固定为：多头对应`LONG_BUILD`、空头对应`SHORT_BUILD`；期权确认要求方向同向、`factor_status=READY`且流动性分数不低于20。
- 连续两日确认只能在第二日收盘后视为可知，禁止回填第一日；轨迹表不含forward outcome，5D/20D标签物理分离并只作历史后验研究。
- Fisher检验与FDR校正使用episode级样本；FDR显著结果还必须覆盖至少3个可比年度、每年状态组和对照组各不少于2个样本，且年度方向一致性不低于75%。
- 2021-03-19至2026-08-07首轮正式研究包含56个episode，55个成熟5D标签和54个成熟20D标签；28次期权在突破前或当日完成确认，16次在T+20窗口内始终未确认。
- 唯一同时通过FDR和年度稳定性门槛的后验研究假设是：20D内全链持仓始终未确认的17个成熟样本命中率29.41%，同周期其余样本64.86%，命中差-35.45个百分点、平均方向收益差-5.64个百分点、q值约0.042；5个可比年度相对同年对照均为负。
- “期权始终未确认”在20D成熟样本中命中率33.33%，对照61.54%，但q值约0.269，只能保留为`WATCH_NEGATIVE`；期权本身尚不能单独成为趋势过滤器。
- 严格等待期权确认会漏掉7个5D和5个20D历史成功突破，说明期权确认具有机会成本；该反事实收益不构成进场规则。
- “20D内始终未确认”只有T+20后才可知，因此所有R93L候选的`promotion_eligible`固定为false；下一阶段只能研究T+1/T+3/T+5/T+10当时可知的动态检查点。
- R93L不修改signal matrix、`composite_score`、策略方向或目标手数；研究型负向候选不得解释为自动反转做空或自动否决。
- 默认进入周更研究包，也可通过`-RunTrendConfirmationTiming`单独运行；不进入日更重链路。
- CLI：`research build-cf-trend-confirmation-timing-research`。

### R93M 期权市场成熟度与趋势确认检查点研究

- 禁止把2021-2026期权样本直接混为一个稳定市场。年份阶段固定分为：2021 `EARLY_THIN`、2022-2023 `EXPANSION`、2024-2026 `MATURE_ACTIVE`。
- 同时使用截至T日的60个交易日期权总成交量、总持仓量中位数，相对冻结的2021基准形成数据驱动阶段；扩张门槛为成交1.5倍且持仓1.25倍，成熟门槛为成交3.0倍且持仓2.5倍。
- 年份阶段与滚动活跃状态分别检验；两者不一致时保留`DIVERGENT`，不得用年份标签覆盖当时真实流动性状态。
- 检查点固定为T、T+1、T+3、T+5、T+10。特征只读取对应检查点及以前的轨迹，检查点后的5D/20D收益物理分离为历史后验标签。
- 2021-01-04至2026-08-12首轮正式研究覆盖56个独立episode，年份阶段事件数为11/16/29，生成277条检查点特征和435条后验验证记录；5个事件的年份阶段与滚动活跃状态不一致。
- 2021、2022-2023、2024-2026的日成交中位数分别约为2.87万、6.41万至17.28万、10.58万至19.67万；日持仓中位数分别约为13.42万、20.00万至31.97万、41.62万至49.94万，支持分阶段研究而非全样本合并。
- 成熟活跃期20D的T至T+10检查点暂呈`WATCH_NEGATIVE`，但FDR后正向和负向显著假设均为0。该结果只说明“期权更活跃不等于同向确认更有效”，不得解释为反向Alpha或自动做空依据。
- R93M不修改signal matrix、`composite_score`、策略方向或目标手数；`promotion_eligible`与`realtime_rule_eligible`固定为false。
- 默认进入周更研究包，也可通过`-RunOptionMaturityConfirmation`单独运行；不进入默认日更链路。
- CLI：`research build-cf-option-maturity-confirmation-research`。

### R93N 动态期权墙与期货结构事件

- 已实现逐日可观察的Call/Put墙距离、持仓变化、墙迁移、接近、触及、突破、建仓/撤仓和区间变化事件，事件特征不读取未来收益。
- 公开期权持仓无法识别买卖方、产业身份或dealer gamma，因此事件方向只作为研究proxy，不直接写回期货方向。
- R93N只提供结构事件和当前映射，不修改signal matrix、`composite_score`或策略仓位。

### R93O 期权墙因子v2

- 已实现固定、可解释候选与期货基准、R48、R93N的统一增量检验，使用1D/3D/5D后验标签、FDR和成熟活跃期留一年验证。
- 截至2026-08-21的正式运行生成1,366条特征、16个固定候选和192条候选证据；成熟活跃期为`KEEP=0`、`WATCH=15`、`REJECT=33`。
- 当前结果是有效的负向晋级结论：期权墙可以解释结构，但尚未证明稳定交易增量，不继续赛后挖掘阈值。

### R93P 期权事件节奏与路径

- 已实现T+1/T+3/T+5固定检查点的延续、反转、MFE、MAE、事件共现和首次解决时间研究。
- 事件轨迹与独立episode统计物理分离，并按市场阶段、到期桶和01/05/09主力周期分层，采用五个交易会话purge的留一年验证。
- 首轮全历史结果没有稳定预测候选；R93P保留为历史解释框架，不进入方向和仓位。

### R93Q 期货—期权市场阶段交互研究

- 已实现固定市场阶段：2021 `EARLY_THIN`、2022-2023 `EXPANSION`、2024-2026 `MATURE_ACTIVE`，并与01/05/09主力周期、到期桶、移仓状态、棉花年度和趋势阶段交叉检验。
- 同合约、同方向、同事件类型的连续事件压缩为episode，仅首个可观察事件进入统计；后续5个交易会话的事件链只用于解释。
- 主结果按事件族检验，事件类型细分结果物理隔离为探索表；基本面与政策只进入具名上下文，不进入方向检验。
- 截至2026-08-21的正式运行包含3,252条原始事件、2,066个独立episode、6,198条后验记录、2,313条主交互和4,401条探索交互。
- `READY=0`、`WATCH=0`、稳定交互候选为0。2021与2022-2023的Call墙1D主效应方向相反，不能外推为2024-2026成熟期交易因子。
- R93Q已接入CLI和周更可选链路，不进入默认日更，不修改signal matrix、`composite_score`、策略方向或仓位。
- CLI：`research build-cf-futures-option-regime-interaction-research`。

### R93R 统一证据门控与停止决策

- 下一任务只汇总期货基准、R48、R93N、R93O、R93P和R93Q的样本量、收益、命中、MFE/MAE、解决周期、FDR、留一年稳定性及成本敏感性。
- 输出统一为`KEEP/WATCH/REJECT`；若没有候选通过成熟期和样本外门槛，停止本轮CF期权因子扩张，保留解释性结构研究。
- R93R不得通过重新搜索阈值制造候选，也不得修改现有方向或仓位。

## 五、输出边界

所有 Markdown 报告使用中文，核心策略代码使用中文注释。每份策略报告和影子摘要必须包含：

> 研究仿真、前向记录、无未来函数，不构成交易指令；NAV 为研究记账值，非真实资金。

## 六、多品种门槛

R94-R99 暂停。只有 CF 历史证据、事件生命周期、期货—期权联动和样本外候选验证通过，
并再次获得明确审批后，才允许制定 SR/TA/MA 扩展任务。

2026-08-23对棕榈油P的进入评估为`CONDITIONAL_GO_P0`：iFinD真实行情和DCE交易日历入口已验证，
但仓库尚无P产品规则、DCE raw/core、全合约链、换月逻辑和品种专属研究口径。允许在明确审批后建立与CF
物理隔离的P0数据与研究基座；这不等于解除R94-R99生产扩展门槛，也不允许直接复制CF期权逻辑。
评估依据见`docs/PALM_OIL_QUANT_TRACKING_ENTRY_ASSESSMENT_2026-08-23.md`。

## 七、工程纪律

- 每个 R 模块独立验证和提交。
- 修改后必须运行 `py -3.12 -m pytest` 和 `py -3.12 -m ruff check src tests`。
- 回测改动必须运行 T+1、no-look-ahead、真实合约和换月成本 golden 测试。
- 数据、规则或结果异常时必须显式 warning 或停止，禁止静默猜测。
